bind 0.0.0.0
protected-mode yes
port 6379

requirepass Redis@123456

# AOF 持久化,每秒刷盘一次,性能与可靠性平衡
appendonly yes
appendfsync everysec

# 缓存场景:限制内存,满了淘汰最久未使用的 key
maxmemory 512mb
maxmemory-policy allkeys-lru